[project]
name = "roli-country-profiles"
version = "1.0.0"
description = "Data parser and static dashboard for WJP Rule of Law Index country profiles"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "openpyxl==3.1.5",
  "pandas==3.0.3",
]

[tool.uv]
package = false
